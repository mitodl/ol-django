{
  "allowedPostUpgradeCommands": [
      "^uv sync",
      "^uv .+"
  ],
  "postUpgradeTasks": {
    "commands": [
      "uv run changelog create-renovate -m 'Updated `{{{depName}}}` from {{{currentVersion}}} -> {{{newVersion}}}'",
      "uv sync"
    ],
    "executionMode": "update"
  }
}
